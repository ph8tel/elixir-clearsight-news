#!/bin/sh
# Runs Ecto migrations in production without Mix installed.
# Called by fly.toml [deploy] release_command = '/app/bin/migrate'
cd -P -- "$(dirname -- "$0")/.."
exec bin/clearsight_news eval "ClearsightNews.Release.migrate"
