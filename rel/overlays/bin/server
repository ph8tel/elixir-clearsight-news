#!/bin/sh
# Starts the application in the foreground with HTTP enabled.
# Called by the Dockerfile CMD ["/app/bin/server"].
# PHX_SERVER=true tells runtime.exs to set server: true on the Endpoint.
cd -P -- "$(dirname -- "$0")/.."
exec env PHX_SERVER=true bin/clearsight_news start
